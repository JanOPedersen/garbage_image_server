model_id: cigarette-butt-v1
version: 2026-03-01
task_type: object_detection
framework: onnxruntime
device: cpu

weights_path: cigarette-butt/cigbutts_yolo11n.onnx

input_name: images
input_format: nchw
input_dtype: float32
normalize: true
channel_order: rgb
input_size: [512, 512]

ort_providers:
  - CPUExecutionProvider

output_decoder: ultralytics_yolo
output_map:
  raw_detections: output0

labels:
  - cigarette_butt
num_classes: 1

default_score_threshold: 0.5
nms_iou_threshold: 0.45
max_detections: 100

max_image_side: 2048
sync_allowed: true